; RF probe: bypasses the modem firmware entirely.
; Same radio settings as src/radio.cpp, but blocking TX and all output on USB.
[env:mkrwan1310]
platform = atmelsam
board = mkrwan1310
framework = arduino
lib_deps =
    SPI
    sandeepmistry/LoRa@^0.8.0
build_flags = -Wall -Wextra
